{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ttrpg-sheet.local/schemas/content/pf1e/classes.schema.json",
  "title": "PF1e class catalog",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "name",
      "hitDie",
      "babProgression",
      "saves",
      "skillPointsPerLevel",
      "classSkills"
    ],
    "properties": {
      "id": { "type": "string", "minLength": 1 },
      "name": { "type": "string", "minLength": 1 },
      "hitDie": { "type": "integer", "minimum": 4, "maximum": 12 },
      "babProgression": { "enum": ["full", "threeQuarter", "half"] },
      "saves": {
        "type": "object",
        "additionalProperties": false,
        "required": ["fort", "ref", "will"],
        "properties": {
          "fort": { "enum": ["good", "poor"] },
          "ref": { "enum": ["good", "poor"] },
          "will": { "enum": ["good", "poor"] }
        }
      },
      "skillPointsPerLevel": { "type": "integer", "minimum": 0 },
      "classSkills": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 }
      },
      "spellsPerDay": {
        "description": "20 class-level rows (1–20), each 10 spell levels (0–9). null means the class cannot cast that level yet. 0 means the table lists 0 (bonus spells still apply).",
        "type": "array",
        "minItems": 20,
        "maxItems": 20,
        "items": {
          "type": "array",
          "minItems": 10,
          "maxItems": 10,
          "items": { "type": ["integer", "null"], "minimum": 0 }
        }
      },
      "source": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "book": { "type": "string" },
          "page": { "type": ["integer", "null"] },
          "rarity": { "enum": ["common", "uncommon", "rare", "unique"] }
        }
      }
    }
  }
}
